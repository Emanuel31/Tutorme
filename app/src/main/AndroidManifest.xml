<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.globomed.tutorme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Common.AllSubjects"></activity>
        <activity android:name=".Student.StudentDashboard" />
        <activity android:name=".Common.OnBoardingActivity" />
        <activity android:name=".Tutor.TutorRegisterActivity" />
        <activity android:name=".Tutor.TutorLoginActivity" />
        <activity android:name=".Student.StudentRegisterActivity" />
        <activity android:name=".Tutor.TutorSignUpActivity" />
        <activity android:name=".Student.LoginActivity" />
        <activity android:name=".Student.StudentSignUpActivity" />
        <activity android:name=".Common.ChooseCategoryActivity" />
        <activity android:name=".Common.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>